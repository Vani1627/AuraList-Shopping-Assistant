    Flask==2.3.2
    firebase-admin==6.3.0
    pandas==2.2.2
    gunicorn==22.0.0
    # Pinning spaCy and its core dependencies to versions known for stable builds on Linux environments
    spacy==3.4.4
    thinc==8.1.10
    blis==0.9.0
    # If your recommender.py uses scikit-learn, add it explicitly
    # scikit-learn==1.3.0
    